# NextGen AI — Manager Dashboard

Business management dashboard for NextGen AI Platform owners and managers.

## Quick Start
```bash
npm install
npm run dev -- --port 8081
```

Connects to backend at `http://localhost:8000`.
